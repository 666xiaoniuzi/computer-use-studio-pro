# 更新记录

## 0.7.5 - 2026-08-29

- 远程任务计量器新增精确墙钟计时：在任务契约接受后、首次远程观察前启动，经过客户接管、等待、重连、验证、双侧清理和宿主机桌面回交后停止。
- 远程完成报告在 Token 用量旁显示开始时间、结束时间、毫秒值及 `HH:MM:SS.mmm` 总时长；普通对话和本机任务收尾保持精简。
- Token 与时长复用同一个内存计量器，并在回交后一次性生成最终快照，不增加截图、GUI 调用或模型往返。

## 0.7.4 - 2026-08-29

- 新增零额外模型往返的跨任务验证剧本缓存：首次远程观察后在同一持久运行时单元内匹配，最多返回两个脱敏语义候选，正常执行只采用最高分候选的首个区分检查。
- 仅在 `success_verified=true` 后记录成功轨迹；首次进入候选、第二次同类成功升级为可信，命中后失败会降权，重复失败自动退役。
- 匹配指纹使用问题类别、操作系统/应用版本桶、远程客户端与界面类型；设备 ID、账户、密钥、剪贴板、完整路径、截图、坐标、窗口句柄和元素索引不会进入缓存。
- 缓存数据位于源码树之外，F盘目录联接安装默认使用 `F:\computer-use-studio-pro\state\verified-playbooks.json`，发布包不携带客户运行数据。

## 0.7.3 - 2026-08-28

- 新增交付物语义命名约束：创建前根据任务目标或文档标题生成最终文件名，清理目标系统禁用字符并保留扩展名；保存后校验确切名称，通用默认名不再视为完成。
- 新增 `deriveArtifactFileName` 及回归测试，远程 Unicode 直接输入不稳定时要求使用剪贴板粘贴或等价可靠路径，并复核桌面文件名。
- 新增零额外往返的 `createTaskUsageMeter`：复用已输出的紧凑视图累计字符、工具调用和截图；远程最终报告优先显示宿主精确 Token 计数，缺少时显示明确标注的紧凑视图估算值。
- 远程收尾新增宿主机可见回交：完成验证与双侧清理后结束 Agent 输入和任务租约，最小化或关闭 ToDesk/向日葵窗口，并在宿主机桌面可见后再发送完成报告。

## 0.7.2 - 2026-08-28

- 新增 `signalUserInputComplete` 与 `resumeAndContinue`。客户完成事件可在持久运行时内触发短防抖、轻量窗口锁检查、一次无截图紧凑观察及匹配后的预备续步。
- `pauseForUserInput` 支持预登记 `returnExpect`、静态 `steps` 或本地 `buildSteps`；显式匹配的完成事件到达前保持客户控制权，返回状态不匹配时跳过续步并交给诊断。
- 稳定回交路径复用原授权租约，默认400字符结果、零截图并节省一次模型往返；失败事务可关闭自动截图提升，避免异常路径产生隐藏的视觉上下文成本。
- 加入回归测试，覆盖缺少完成信号、事件ID、客户控制权、窗口轻量检查、授权复用、零截图、紧凑 `tokenView` 和验证续步。
- 默认Codex/Windows指令链由18,172降至18,071字节，远程链由26,205降至26,201字节；新增能力未增加默认指令体积代理值。

## 0.7.1 - 2026-08-28

- 将默认加载链改为精简 `SKILL.md`、单个 `fast-contract.md`、当前适配器与当前界面片段；详细核心工作流、恢复和性能资料按需加载。
- 新增 `tokenView`：完整观察与工具结果留在持久 JavaScript 运行时，同一执行单元只向模型返回状态、脱敏紧凑证据、必要指标及有限变化信息。
- `compactState` 的日常默认预算由 1800 字符调整为约 900 字符，并支持 400/900/1800 的稳定轮询、日常决策与恢复分级；截图摘要默认只保留最上层区域。
- 保留持续会话授权、设备锁、断线恢复、观察租约、客户密码/验证码/支付接管及 API 创建配置测试流程；完整密钥保持在私密输入或运行时路径并清理任务痕迹。
- 默认 Codex/Windows 指令链按 UTF-8 文件字节由 33,334 降至 18,172（45.5%）；加入远程模式后由 48,431 降至 26,205（45.9%）。
- 扩充自检，覆盖紧凑结果不包含原始 `state`、`observation_chars`、完整密钥或多余截图。

## 0.7.0 - 2026-08-28

- 新增 `createRemoteClientSignalAdapter`，在持久 JavaScript 运行时内直接解析 ToDesk/向日葵的连接、重连、断线、急停和设备 ID 信号；稳定状态无需模型再次判断。
- 远程客户端适配器采用初始设备证据、基线设备集合和冲突 ID 检测：设备 ID 临时隐藏时保持绑定，新出现的设备 ID 会锁止输入。
- 持久会话新增坐标、焦点和语义三类观察租约。过期状态会在输入发生前返回 `STALE_OBSERVATION_LEASE`，避免高负载任务复用陈旧坐标、焦点或元素索引。
- 会话结果与快照新增输入租约、观察年龄和租约预算；普通远程输入继续只读取缓存授权门，不新增逐输入远程验证往返。
- 增加 ToDesk/向日葵中文状态、设备冲突、断线、急停和确定性过期时钟回归测试。

## 0.6.1 - 2026-08-28

- 收紧键盘 Burst 文本输入：空值、缺失值、控制字符、换行/制表和超长文本会转入逐动作验证路径，避免在单字段快速路径中触发表单提交或焦点跳转。
- 修复远程设备 ID 在初始绑定后暂时离开无障碍文本时被误判为设备切换的问题；初始连接和重连仍要求设备证据，后续观察若出现不同的标记设备 ID 仍会锁止会话。
- 补充窗口枚举等待参数与返回类型校验，并新增相应的边界回归测试。

## 0.6.0 - 2026-08-27

- 新增严格限定的 `runKeyboardBurst`：在已验证焦点、稳定单字段、普通低风险键盘序列中，将两到三次输入合并为一次终态观察；语义验证直接完成，视觉模式返回一张终态截图供模型复核。
- 持久本机与远程会话新增 `session.keyboardBurst`，远程模式仍在每次输入前读取本机缓存授权门，断线、急停和设备锁规则保持有效。
- 新增 `waitForWindowListState`，使用低开销窗口枚举验证应用出现或关闭，避免为纯窗口生命周期条件支付完整截图/无障碍捕获成本。
- 实测当前 Windows Computer Use 状态捕获中位数约为：无截图语义观察 3.056 秒、截图观察 3.153 秒、窗口枚举 0.018 秒；优化重点转为减少状态捕获次数。
- Windows 文件资源管理器可见搜索框三轮对照中，两次逐动作截图的基线中位数为 6.305 秒，`runKeyboardBurst` 单次终态截图中位数为 3.220 秒，下降 48.9%；三轮结果均返回终态截图供视觉复核。

## 0.5.9 - 2026-08-27

- 显式启用隐式调用策略；用户无需输入 `$computer-use-studio-pro`，通过“操控本机”“控制电脑”“界面操作”“远程操控”“ToDesk”“向日葵”等自然语言意图即可自动加载。
- 增加本机与远程模式自动路由：目标为宿主机时选择 `local`，目标为远程客户端内的另一台电脑时选择 `remote-fast-fix`。
- 远端操作系统未提供时在首次完整观察中识别并缓存；仅讨论方案或完全不触发 GUI 输入的文件/API 任务保持原有专用路线。

## 0.5.8 - 2026-08-27

- 将 `computer-use-studio-pro` 固化为每次 Computer Use / `@oai/sky` 操作的必经编排层；显式调用本 Skill 或实际选择 GUI 控制路线时均加载。
- Codex 适配器现在要求在首个输入前导入并复用 `sky_fast_path.mjs`，优先使用持久运行时、紧凑观察、动作后刷新和本地验证事务，而不是重复的原始截图往返。
- 修复 Codex 持久 `node_repl` 不暴露全局 `process` 时快速路径模块导入报错的问题，同时保留 Node CLI 自测入口。
- 本机明确任务中的普通低风险可逆操作采用任务级持续授权，不再加入逐点击、逐按键或逐窗口的重复询问；高风险与宿主动作时确认边界保持独立。
- 明确本地安装内容是执行时事实来源，常规任务无需重新访问 GitHub；上游仓库继续作为源码与发布来源。

## 0.5.7 - 2026-08-27

- 修复通过 Windows 目录联接或符号链接安装后，`sky_fast_path.mjs --self-test` 因入口路径指向 C 盘、模块真实路径位于 F 盘而静默退出的问题。
- CLI 入口现在比较两侧真实路径；C 盘只保留链接时也可直接运行完整自测。

## 0.5.6 - 2026-08-27

- 将远程模式默认操作对象设为 `entire-bound-device`：覆盖绑定客户电脑中的全部桌面、磁盘、系统设置、应用程序、终端、服务、网络组件和注册表。
- 将网络、代理、Git、证书、DNS 与 Codex 配置明确为诊断起点，而非子系统白名单；具体任务目标继续作为完成与验证边界。
- 在会话快照、操作结果和目标指纹中加入 `operation_scope`，便于宿主确认当前是整机模式。
- 更新中英文调用模板、Codex 适配说明和行为回归测试。

## 0.5.5 - 2026-08-27

- 将持续授权调整为连接级会话租约：建连时确认一次，Agent 连续控制期间每次输入只读取本机缓存状态，不再调用远程授权、连接、设备或急停验证器。
- 远程信号改在完整/增量观察、ToDesk/向日葵客户端事件和重连节点复核，断线、急停及设备/窗口锁变化仍会撤销租约。
- 新增 `pauseForUserInput` 与 `resumeAgentControl`：客户输入密码、验证码或 UAC 凭据时暂停 Agent 输入、保留租约，交还控制后刷新一次界面和焦点映射即可续跑。
- 增加回归测试，验证多次输入门检查不触发远程验证器、客户输入交接不重复授权。

## 0.5.4 - 2026-08-27

- 新增持续授权状态：远程连接建立后保持有效，断线、客户急停、目标锁变化时立即撤销输入授权。
- 将客户设备 ID 升级为远程模式必填锁，和 ToDesk/向日葵窗口句柄、应用与标题共同在每次观察和输入前校验。
- 新增 `connected/stalled/disconnected/connected-unauthorized/rebinding/stopped` 恢复状态，以及同设备重新授权、完整重映射和从最后验证检查点续跑。
- 修复旧截图 ID 可被复用、未验证动作被标记为已验证、最终成功条件未由快速路径执行的问题。
- 加固本机任务文件清理：加入台账锁、结构校验、调用方根目录绑定，并在任务根仍有未登记内容时报告 `cleanup_pending`。

## 0.5.3 - 2026-08-27

- 将结束清理扩展为本机控制端与远程端双侧流程，重点清理本次任务在本机生成的非必要文件。
- 精确定义临时、任务无关、回滚和交付物四类文件；既有文件及最终结果保持原状。
- 新增 `task_artifacts.py`，只在任务专属本机目录内执行经过路径校验的清理，并提供自检。
- 完成顺序调整为：结果验证、远程清理、断开连接、本机清理、最终报告。

## 0.5.2 - 2026-08-27

- 为远程模式加入默认的会话临时文件生命周期和精确归属台账。
- 任务结束时先清理本次会话创建的临时文件与已失效回滚副本，验证后再断开ToDesk/向日葵。
- 明确保留用户交付物、既有文件、安装结果和缺少归属证据的路径，避免宽范围清理。
- 将清理状态加入远程任务完成报告和默认提示。

## 0.5.1 - 2026-08-27

- 强化远程目标指纹：加入应用、窗口句柄、标题提示、可选会话身份提示和自定义验证器。
- 加入语义变化 epoch、自动截图提升及远程视频画布的显式内容变化提示。
- 扩充错误目标、语义变化、截图提升和窗口重绑定回归测试。
- 新增确定性发布构建器，分别生成排除 `.git` 的源码包、精简安装包和 SHA-256 校验文件。

## 0.5.0 - 2026-08-27

- 将 `remote-pc-fast-fix` 合并为 `computer-use-studio-pro` 的按需 `remote-fast-fix` 模式，只保留一个 Skill 入口与一个实际 Computer Use 执行链。
- 增加持久窗口会话：首次完整观察、日常紧凑刷新、失败/验证截图、最多三个逐步验证动作和两次同路径失败后的诊断切换。
- 增加 ToDesk/向日葵目标锁、任务范围、成功标志、布局 epoch、远程输入检查、回滚栈与自适应等待规则。
- 更新中英双语快速开始、清单、Codex 适配说明和界面元数据。

## 0.4.2 - 2026-07-20

- 修正 Codex 手动安装目录与 `skills` CLI 的 Node.js 版本要求。
- 强化 OOXML 文本验证，避免跨文本节点或非文本 XML 内容导致误报。
- 修复 UI 差异输出中的敏感路径泄漏、状态初始化边界和本地事务断言检查。
- 扩充自检，统一版本号与文档说明。

## 0.4.1 - 2026-07-20

- 新增中英双语 README、语言互链、编号标题和目录。
- 更新 GitHub 发布说明与跨 Agent 安装入口。
- 将横幅升级为包含功能说明的项目主页图。

## 0.4.0 - 2026-07-20

- 重构为跨智能体通用核心与独立适配层。
- 将 Codex Windows 专用快速路径移至 `adapters/codex/`。
- 新增 Codex、Claude Code、Hermes、OpenClaw 与通用适配说明。
- 重写根目录中文 README，按不同 Agent 分别说明安装方式。
- 补充安全策略、贡献指南与历史文档归档。
