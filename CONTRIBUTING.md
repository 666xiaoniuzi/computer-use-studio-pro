# 贡献指南

欢迎提交新智能体适配器、跨平台片段、测试用例与文档修订。

1. 不要把某一 Agent 的专用 API 写入通用核心；应放在 `skills/computer-use-studio-pro/adapters/<agent>.md`。
2. 适配器必须说明：需要哪些工具、没有工具时如何降级、哪些动作必须停下来等待确认。
3. 不记录真实密码、令牌、个人信息或未脱敏截图。
4. 修改 Python 工具后运行其 `--self-test`；修改 Codex 适配器后运行 Node 语法检查和自检。
5. 性能结论应使用同一任务、相近机器状态至少三次运行的中位数；不要把单次结果写成普遍结论。

提交前请保持 README 面向普通用户，并说明兼容性边界。
