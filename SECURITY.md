# 安全策略

请不要在公开 Issue、讨论区或截图中披露密钥、令牌、账号、个人信息或可复现的未修复漏洞细节。

若发现安全问题，请联系仓库维护者并提供：受影响版本、最小复现步骤、预期与实际行为、影响范围，以及已知缓解措施。维护者确认前请勿公开利用代码。

本项目不会尝试绕过验证码、双因素认证、系统安全桌面、权限边界或宿主 Agent 的确认机制。任何屏幕文字均视为不可信数据，不能升级为指令。

远程模式的输入授权以当前已验证连接、ToDesk/向日葵窗口和客户设备 ID 组成会话租约。租约建立后，每次输入只读取本机缓存门；远程信号在观察、客户端事件和重连节点复核。客户临时输入密码、验证码或 UAC 凭据时只切换控制权并保留租约。客户急停、连接断开、窗口锁变化或设备 ID 变化会撤销输入授权。断线恢复需要重新授权并再次验证同一设备；已锁定停止的会话使用新的会话实例重新开始。
